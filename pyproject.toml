[build-system]
requires = [
    "setuptools>=38.6.0",
    "wheel>=0.31.0",
    "twine>=1.11.0"
]
build-backend = "build"
backend-path = ["src/ppbt"]

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
